name: CI

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1
    - name: TestCafe
      uses: AlexKamaev/testcafe-docker-action@master
      with:
        args: "chromium tests --dev"


